apiVersion: v1
kind: Service
metadata:
  name: front
spec:
  # LoadBalancer => EKS crea un ELB publico. Esa es la URL de la app.
  type: LoadBalancer
  selector:
    app: front
  ports:
    - port: 80
      targetPort: 80
