apiVersion: v1
kind: Service
metadata:
  name: back-ventas
spec:
  # Interno. El front (nginx) lo alcanza por el nombre DNS "back-ventas".
  selector:
    app: back-ventas
  ports:
    - port: 8080
      targetPort: 8080
